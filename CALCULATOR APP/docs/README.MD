# Calculator App

A simple calculator app built with Next.js and TypeScript that performs basic arithmetic operations like +, -, *, /, %, ^ and more.

## How to Run
clone the repo, run `npm install` then `npm run dev` and open http://localhost:3000

## Screenshots
![Calculator UI](docs/screenshot.png)